{
  "name": "epic-tech-ai-interactive",
  "version": "1.0.0",
  "description": "An interactive webpage showcasing the capabilities of Epic Tech AI with stunning audio and visual elements.",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_OPTIONS=--openssl-legacy-provider react-scripts start",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider react-scripts build",
    "test": "cross-env NODE_OPTIONS=--openssl-legacy-provider react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "styled-components": "^5.3.10",
    "three": "^0.155.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "homepage": "https://your-username.github.io/epic-tech-ai-interactive",
  "author": "Epic Tech AI",
  "license": "ISC",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
